% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coint_maki.R
\name{plot.maki_test}
\alias{plot.maki_test}
\title{Plot a Maki Cointegration Test Result}
\usage{
\method{plot}{maki_test}(x, ...)
}
\arguments{
\item{x}{A \code{"maki_test"} object from \code{\link{coint_maki}}.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object (invisibly), or \code{NULL} if 'ggplot2' is not
  installed.
}
\description{
Draws a two-panel dashboard with 'ggplot2': the dependent variable with its
break-adjusted long-run fit and shaded regimes (top), and the cointegrating
residual about zero (bottom), with dashed lines at the estimated breaks.
}
\examples{
set.seed(1)
n <- 60
x <- cumsum(rnorm(n))
y <- 2 + 3 * x + 4 * (seq_len(n) > 30) + rnorm(n)
res <- coint_maki(cbind(y, x), m = 1, model = 2)
if (requireNamespace("ggplot2", quietly = TRUE)) plot(res)
}
